<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h2>Gruppen</h2>
    <%= f.label :groups %><br />
    <%= f.text_field :groups %>
    <br/>
    Jede Gruppe bringt Rechte. Die mächtigste ist „root“. Mehrere Gruppen mit Leerzeichen trennen.
  </div>

  <div class="field">
    <h2>Fachschaften</h2>
    <p>Fachschaften, in denen <%= @user.name %> aktiv ist. Ein Häkchen bedeutet, dass <%= @user.name %> für die jeweilige Fachschaft abstimmen darf und deren Daten ändern kann.</p>
  <% for fachschaft in Fachschaft.all %>
    <label>
    <%= check_box_tag "user[fachschaft_ids][]", fachschaft.id, @user.fachschaften.include?(fachschaft) %>
    <%= fachschaft.name %></label>
  <% end %>
  </div>



  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
