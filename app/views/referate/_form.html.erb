<%= form_for(@referat) do |f| %>
  <% if @referat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@referat.errors.count, "error") %> prohibited this referat from being saved:</h2>

      <ul>
      <% @referat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :mail %><br />
    <%= f.text_field :mail %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
